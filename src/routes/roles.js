// import { Router } from "express";

// import {
//   addRole,
//   getRoleById,
//   getRoles,
//   updateRole,
//   deleteRole,
// } from "../controllers/roles";
// import { validate, roleSchema } from "../utils/validations";

// const router = Router();

// router.get("/", getRoles);
// router.get("/:id", getRoleById);

// router.post("/", validate(roleSchema), addRole);

// router.put("/:id", validate(roleSchema), updateRole); // Update role is missing

// router.delete("/:id", deleteRole);

// export default router;
